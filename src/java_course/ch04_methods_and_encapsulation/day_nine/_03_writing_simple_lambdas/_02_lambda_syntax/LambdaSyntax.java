
(Animal a) > { return a.canHop(); }

/*
 * La sintaxis lambda es la sigueinte:
 * Tipo de parámetro opcional: Animal
 * Nombre del parámetro: a
 * Flecha: ->
 * Cuerpo: a.canHop()
 * Requerido en el bloque: return y ;
 * Psdt: 
 * - Los parántesis se puede omitir si solo hay un parámetro.
 * - Se pueden omitir los {}.
 * - Si se omiten los {}, se puede omitir el return y el ;.
*/